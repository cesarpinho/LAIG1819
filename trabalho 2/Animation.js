/**
 * Animation
 * @constructor
 */

class Animation
{
    constructor(scene, id, span) {
        this.scene = scene;
        this.id = id;
        this.span = span;
    };

    update(time){
    };

    apply(){
    };
};
