/**
 * Animation
 * @constructor
 */

class Animation
{
    constructor(scene, id, time) {
        this.scene = scene;
        this.id = id;
        this.time = time;
    };

    update(time){

    };

    apply(){
    };
};
