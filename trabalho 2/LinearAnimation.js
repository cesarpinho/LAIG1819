/**
 * 
 */

 class LinearAnimation extends Animation 
 {
    constructor(scene, points, time) {
        super(scene);

        this.scene = scene;
        this.points = points;
        this.time = time;
    }
 }